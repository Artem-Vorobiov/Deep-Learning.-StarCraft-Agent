import os
var = 'deleter.py'

# MODEL_NAME   = 'BasicCNN-10-epochs-0.0001-FIRST'

# if os.path.exists('{}'.format(MODEL_NAME)):
#     model.load(MODEL_NAME)
#     print('model loaded

if os.path.exists('{}'.format(var)):
	print('\n\t\t\tGotcha -->')